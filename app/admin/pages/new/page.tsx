'use client';

import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';

export default function NewPage() { const router = useRouter(); const [title, setTitle] = useState(''); const [slug, setSlug] = useState(''); const [error, setError] = useState(''); const submit = async (event: FormEvent) => { event.preventDefault(); setError(''); const response = await fetch('/api/admin/pages', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ title, slug }) }); const result = await response.json(); if (!response.ok) { setError(result.error || 'Não foi possível criar a página.'); return; } router.push(`/admin/pages/${result.id}/edit`); }; return <main className="admin-page-list"><div className="admin-form-card"><p className="eyebrow">CMS visual</p><h1>Nova página</h1><form onSubmit={submit}><label>Título<input value={title} onChange={event => setTitle(event.target.value)} placeholder="Ex.: Sobre a Odontoart" required /></label><label>Slug<input value={slug} onChange={event => setSlug(event.target.value)} placeholder="sobre-a-odontoart" required /></label>{error && <p className="form-error">{error}</p>}<div className="form-actions"><button className="admin-primary">Criar página</button><button type="button" onClick={() => router.push('/admin/pages')}>Cancelar</button></div></form></div></main>; }
